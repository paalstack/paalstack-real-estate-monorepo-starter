{
  "name": "@starter/api-types",
  "version": "0.1.0",
  "private": true,
  "description": "Real Estate Starter — Shared TypeScript types and Zod schemas for the 9 NestJS modules. Re-exports Prisma-generated model types from @starter/database and provides request/response DTOs as Zod schemas (inferred TypeScript types via z.infer). One source of truth for validation in NestJS pipes AND Next.js route handlers.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "src", "README.md"],
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@starter/database": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}